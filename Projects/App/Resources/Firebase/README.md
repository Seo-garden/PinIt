Add the Firebase configuration file downloaded from the Firebase console here:

- `Projects/App/Resources/Firebase/GoogleService-Info.plist`

This file is required by the `App` target at runtime. It should not be placed in the `Presentation` framework.
